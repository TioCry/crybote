const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
               message.channel.send('bot under developement, wait for me to add commands.');
}

module.exports.help = {
  name:"help"
}
